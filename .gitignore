node_modules/
dist/
*.log

# Nothing in this project reads a .env file (see .env.example), but keep a
# local one out of git in case it's used for `docker run --env-file`.
.env

# git worktrees for isolated feature work
.worktrees/
.superpowers/
